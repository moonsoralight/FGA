# 2026-09-10 三／六敌人模式工程

- 基线：工作区中的 `../FGA_20260909_NPDetectionFix_Source/`，独立复制后修改，不覆盖原工程。
- 当前产品版本名称：0.1.2；整数版本码：1。
- 构建类型：ci；包名：`io.github.fate_grand_automata.debug`。
- 签名配置沿用基线的 `app/fgadebug.keystore`，不替换签名身份。

## 功能变更

- 每个 Wave 独立保存三／六敌人模式，未设置的面默认三敌人。
  - `autoskill_enemy_modes` 通过原战斗配置存储及导入／导出体系保存，按 Wave 顺序记录 3 或 6。
  - 模式不写入 t 指令，原技能字符串本身不包含模式数据。分享六敌人配置应导出完整战斗配置，而不是只复制技能字符串。
- 三敌人显示 3、2、1；六敌人上排 6、5、4，下排 3、2、1；t1～t6 始终表示对应编号。
- 本面已有手动目标时，任一方向切换模式都会删除本面所有回合的 t1～t6，并提示重新设置；其他动作、其他面模式与指令不变。
- 提示弹窗支持确认按键与外部点击关闭，两种方式均不撤销清理。
- 两种模式使用独立标定点，运行时按当前 Wave 选择；保留点击后等待 0.5 秒及关闭额外信息窗口。
- 自动选敌保留原三个位置、扫描次序和危险状态语义；不扩展六敌人自动选敌。
- 旧脚本的 t1/t3 物理左右指向随用户要求改变，不自动改写已有技能字符串。

## 本地离线构建

- 已有工作区工具位于 `../AndroidBuildEnv/`。
  - JDK 17：`jdk17/jdk-17.0.20+8/`。
  - Android SDK：`sdk/`，平台 35、构建工具 35.0.0。
  - Gradle 9.5.1：`gradle-9.5.1/`。
  - 已缓存的构建依赖：`gradle-cache/`。
- 在 PowerShell 运行 `./build-local.ps1`，使用上述已有工具执行 `:app:assembleCi --offline --no-daemon`。
  - 脚本从自身位置定位相邻工具目录，不依赖盘符。
  - 工具或缓存缺失时报告失败，不自动下载。
  - 输出：`app/build/outputs/apk/ci/`。
- 源码包包含源码、资源、模块配置、Gradle Wrapper、现有模块库及依赖版本说明；不包含完整 SDK、JDK、Gradle 缓存或构建中间文件。
- 其他机器需自行提供对应工具与依赖。Gradle Wrapper 和依赖版本目录已保留，联网获取依赖应由使用者明确决定。

## 验证边界

- 2026-09-10 离线编译成功：`:app:assembleCi --offline --no-daemon`，100 个任务执行，耗时 5 分 32 秒。
- APK 核对结果：版本名称 0.1.2、版本码 1、既有 debug 包名；v2 签名有效，签名证书 SHA-256 与 9 月 9 日 APK 一致：`5acce47e71373dcfe3a6f6400ed3b3f687bd454aab91c7562c8327a3a083ac9d`。
- APK 包含主 DEX、清单和资源表，以及 arm64-v8a／armeabi-v7a 原生库。结构核对不等于实机功能验证。
- 九个点已基于用户提供的 2736×1264 原图标定，脚本整数点反算回原图标注点一致；不等于游戏实机点击成功。
- 六敌人敌人 1 取实际星形职阶框中心，只将外围区域近似为与其他五个同等大小的菱形。
- 非宽屏沿用旧敌人定位的 183 脚本单位横向分支差值；缺少非宽屏实机验证，不宣称所有设备已通过。
- 不新增目标存在／锁定成功检测、死亡或增援后的自动重新锁定、攻击链逐张换目标能力。
- 新增 `EnemyModeTest.kt` 测试源码；执行状态应以工作区交付记录为准，不因文件存在而认为测试通过。
- 2026-09-10 交付时：解析与保存、按面清理与索引、弹窗代码路径、自动选敌隔离、源码差异等专项检查仍待用户许可，未执行项目单元测试或实机测试。
- 原 `RELEASE_20260909.md` 保留为基线历史说明，不代表该文件描述的是 0.1.2 新功能验证结果。
