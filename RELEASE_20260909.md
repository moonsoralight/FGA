# 2026-09-09 源码说明

本目录是 2026-09-09 当前完整源码，包含以下已确认功能。

## 1. 战斗开始前与结束后的对话跳过

- `battle.png` 仍是正式进入 `performBattle()` 的唯一标志。
- 未出现 `battle.png` 且识别到战斗开始特征时，在安全区域单击并返回原有主循环重新检测。
- 进入正式战斗流程后，本场战斗关闭战前对话分支，下一场战斗再重新启用。
- 最后一个实际包含技能操作的 Wave/Turn 完成选卡后，允许检测战斗结束特征。
- 匹配后按设置次数在安全区域点击，并接回原有结算与自动梦火流程。

## 2. 自动使用梦火的地图关卡查找

- 地图模式使用玩家自行截取的“地图关卡特征图”，不再内嵌固定地图入口模板。
- 玩家未选择地图图片时，跳过地图查找并进入原有确认关卡步骤。
- 地图图片与一级、二级入口图片使用相同的加载和设备缩放路径。
- 原有二次确认、等待、偏移点击、失败重试和后续确认关卡流程保持不变。

## 3. 自定义出卡策略的宝具卡检测修正

- N1、N2、N3 使用互相独立的宝具卡头像检测区域。
- 自有从者的检测区域收窄为 220×180，并继续使用 FGA 已缓存的从者头像模板。
- 自有从者匹配不再写死 0.5，相似度改为采用全局匹配度。
- 助战宝具卡继续走独立的 Support 标记检测分支。
- 自定义组合、技能顺序、宝具连发、普通指令卡匹配和补卡逻辑没有改变。

## 构建

项目保留完整 Gradle Wrapper、源码和配置。当前 APK 使用工作区内已有的 JDK 17、Android SDK 35 与 Gradle 9.5.1 构建：

```text
:app:assembleCi --offline --no-daemon
```

生产 APK 构建成功。专项测试源码已经加入；由于本地缓存缺少测试专用依赖，且未获准联网下载，因此没有将专项测试标记为已通过。
